# Assign the input function to I for shorthand usage
I = input

# Execute the code inside exec() based on the number of iterations
exec(int(I()) * "y,h,u = (I().split() for _ in 'owo'); print(('Bob','Alice')[h != u != h[::-1]]);")
